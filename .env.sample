# NowSecure API Configuration Reference
# This is a template showing which environment variables the tool supports
# Copy these variables to your shell environment or use them with 'source'

# Your NowSecure API key (required)
# Get this from your NowSecure account dashboard
NS_API_KEY=your_api_key_here

# NowSecure API endpoint (optional)
# Default: https://api.nowsecure.com/graphql
# Only change if using a different NowSecure instance
# NS_API_ENDPOINT=https://api.nowsecure.com/graphql

# Usage examples:
# 1. Export manually:
#    export NS_API_KEY="your_api_key_here"
#    python check_sbom.py --ref "uuid" --vuln-db vulnerable.txt
#
# 2. Source this file (after editing):
#    cp .env.sample .env
#    # Edit .env with your actual API key
#    source .env
#    python check_sbom.py --ref "uuid" --vuln-db vulnerable.txt